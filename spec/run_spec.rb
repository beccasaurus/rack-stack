require "spec_helper"

describe RackStack, "#run" do

  it "run @app"
  it "run @app, :if => <RackStack::RequestMatcher>"
  it "run @app, :unless => <RackStack::RequestMatcher>"
  it "run @app, :if => <RackStack::RequestMatcher>, :unless => <RackStack::RequestMatcher>"

end
